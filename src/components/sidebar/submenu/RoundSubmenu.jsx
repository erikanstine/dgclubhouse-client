export default function RoundSubmenu({ rounds }) {
    // const courseMap = rounds.map(function(d) {return d.courseName;}).values(); 
  return (
    <div className='sidebarSubmenu'>
      {/* {rounds.map((r) => (
        <>
          <div className='course' id={r.id}>
            <div className='courseName'>{`${r.courseName}`}</div>
            <div className="score">{`${r.score}`}</div>
          </div>
        </>
      ))} */}
      future home of rounds submenu
    </div>
  );
}
